# --- Secrets / env -----------------------------------------------------------
.env
.env.local
*.local

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# --- Models & weights (never commit; baked into the image at build time) -----
*.bin
*.safetensors
*.gguf
*.pt
*.onnx
models/cache/
.hf_cache/

# --- Pipeline artifacts ------------------------------------------------------
input/
output/
out/
tmp/
*.wav
*.mp4
*.mkv
keyframes/

# --- Runtime data (SQLite user store, local API data dir) --------------------
*.db
services/api/data/

# --- IBM Bob / Spec-Kit generated shadow state (created by `specify init` / Bob) --
.specify/cache/
.bob/
*.checkpoint

# --- Node (Track 3 web demo) -------------------------------------------------
node_modules/
.next/
out/
.expo/

# --- Local Claude permissions ------------------------------------------------
.claude/settings.local.json

# --- OS / IDE ----------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
